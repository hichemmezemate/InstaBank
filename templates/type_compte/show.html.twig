{% extends 'base.html.twig' %}

{% block title %}TypeCompte{% endblock %}

{% block body %}
    <h1>TypeCompte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_compte.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_compte.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_compte_index') }}">Retour</a>

    <a href="{{ path('app_type_compte_edit', {'id': type_compte.id}) }}">edit</a>

    {{ include('type_compte/_delete_form.html.twig') }}
{% endblock %}
